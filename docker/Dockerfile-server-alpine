FROM tkanstantsin/userver-php-fpm:4.7-php7.3.13-alpine3.11

# Utils
RUN apk add --no-cache --virtual .build-deps \
            git \
    \
    && apk add --no-cache \
            bash coreutils \
            mc htop curl \
            rsyslog \
    # Fix terminal
    && echo "export TERM=xterm mc" >> ~/.bashrc \
    \
    # Cerbot
    && apk add --no-cache certbot \
    \
    # Supervisor
    && apk add --no-cache supervisor \
    && mkdir -p /var/log/supervisord \
    \
    && apk del .build-deps

CMD ["supervisord", "-n"]
